/**
 * This test file is generated as an example for Jest Testing.
 *
 * If you want to learn more about Jest
 * please visit https://jestjs.io/docs
 */

// @ts-ignore
describe("example test suite", () => {
    it("unit test 1", () => {
        const num = 5;

        expect(num).toEqual(5);
    });
    it("unit test 2", () => {
        const num = 10;

        expect(num).toEqual(10);
    });
});
